name: First GitHub Action

on:
  workflow_dispatch:  # Allows manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1001907@ds.study.iitm.ac.in
        run: echo "Hello, world!"
